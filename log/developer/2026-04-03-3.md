# Developer 交接文件 2026-04-03-3

## 交付對象
Checker

## 前次摘要（2026-04-03-2）
在 `app.py` 加入 `gr.Markdown` 說明區塊，test_app.py 24 個測試全數通過。

## 本次任務
依據 `log/tester/2026-04-03-3.md`，新增威脅情報功能。

---

## 實作細節

### `project/kits/threat.py`（新增）
- `check_poc(cve_id)` — 呼叫 `api.github.com/search/repositories?q={cve_id}`，回傳 `{"has_poc": bool, "poc_count": int}`
- `check_kev(cve_id)` — 呼叫 CISA KEV JSON，比對 CVE ID（不分大小寫），回傳 `{"in_the_wild": bool}`
- `GITHUB_TOKEN`：從 `key.yaml` 讀取（`GITHUB_TOKEN` key），無此 key 或檔案不存在時靜默設為 `None`
- 有 token → `Authorization: token {GITHUB_TOKEN}` header；無 token → 無 Authorization header

### `project/kits/reporter.py`
- 新增 `from kits.threat import check_poc as check_poc_fn, check_kev as check_kev_fn`
- `generate_report` 簽名：`generate_report(cve_id, check_poc=False, check_kev=False)`
- `check_poc=True` → 呼叫 `check_poc_fn`，結果併入 `threat_intel`
- `check_kev=True` → 呼叫 `check_kev_fn`，結果併入 `threat_intel`
- `threat_intel` 非空 → 加入回傳 dict；兩者皆 False → 不含 `threat_intel` 欄位

### `project/app.py`
- `analyze` 簽名：`analyze(cve_id, check_poc=False, check_kev=False)`
- 呼叫 `generate_report(cve_id.strip(), check_poc=check_poc, check_kev=check_kev)`
- 新增兩個 `gr.Checkbox`：
  - `poc_cb = gr.Checkbox(label="偵測 PoC（GitHub Search API）", value=False)`
  - `kev_cb = gr.Checkbox(label="偵測 In the Wild（CISA KEV）", value=False)`
- `btn.click` inputs 改為 `[inp, poc_cb, kev_cb]`
- Markdown 說明區塊新增威脅情報選項說明

---

## 測試結果

| 檔案 | 通過 | 失敗 | 說明 |
|------|------|------|------|
| test_fetcher.py | 15 | 0 | 維持通過 |
| test_threat.py | 16 | 0 | 全數通過 |
| test_reporter.py | 34 | 0 | 含 integration test 全數通過 |
| test_app.py | 34 | 0 | 含 Checkbox UI 測試全數通過 |
| **合計（不含 analyzer/recommender）** | **83** | **0** | |

---

## Checker 需驗證的項目

1. `kits/threat.py`：`check_poc` / `check_kev` 邏輯、GITHUB_TOKEN 靜默處理
2. `kits/reporter.py`：`check_poc_fn` / `check_kev_fn` 引入名稱、`threat_intel` 組裝邏輯
3. `app.py`：`analyze` 三參數、兩個 Checkbox 元件、`btn.click` inputs
4. `python -m pytest tests/test_threat.py tests/test_reporter.py tests/test_app.py`：68 passed
