# Developer 交接文件 2026-04-03-4

## 交付對象
Checker

## 前次摘要
上次完成威脅情報（PoC + KEV）實作，68 個測試通過，commit `9db4dc7` 推送至 `fresh` branch。

---

## 本次實作內容

### 1. `project/kits/threat.py` — 新增 `check_epss()`

呼叫 `https://api.first.org/data/1.0/epss?cve={cve_id}`，回傳：
- 成功：`epss_score`, `epss_percentile`, `epss_high_risk`（門檻 > 0.1）
- 失敗/查無資料：`epss_score=None`, `epss_percentile=None`, `epss_high_risk=False`, `epss_note="無資料"`
- 捕捉所有例外，不往外拋

### 2. `project/kits/reporter.py`

- `generate_report()` 新增 `check_epss: bool = False` 參數
- 新增 `check_epss_fn` import
- `threat_intel` 邏輯加入 EPSS 查詢
- 高風險時（`epss_high_risk=True`）新增 `report["epss_warning"]`
- 新增 `report_to_markdown(report: dict) -> str`：
  - 輸出含標題、基本資訊、影響分析、修復建議
  - `threat_intel` 存在時輸出威脅情報表格
  - `epss_warning` 存在時輸出 `⚠️` 警告

### 3. `project/app.py`

- `analyze()` 新增 `check_epss: bool = False` 參數，傳遞至 `generate_report()`
- 新增 `to_markdown(report_json)` 函式，回傳 `(md_text, gr.update)`
- 新增 `epss_cb`（EPSS Checkbox）
- 新增 `md_btn`（產生 Markdown 報告按鈕）
- 新增 `md_out`（gr.Markdown 輸出）
- 新增 `dl_btn`（gr.DownloadButton，下載 `/tmp/cve_report.md`）
- Markdown 說明新增 EPSS 說明行

---

## 測試結果

```
96 passed, 2 warnings
```

| 檔案 | 通過 |
|------|------|
| test_fetcher.py | 15 |
| test_analyzer.py | 22 |
| test_recommender.py | 20 |
| test_threat.py | 17（+6 EPSS）|
| test_reporter.py | 15（+12 EPSS + Markdown）|
| test_app.py | 7（+11 EPSS + to_markdown + UI）|

---

## 變更檔案
- `project/kits/threat.py`
- `project/kits/reporter.py`
- `project/app.py`

## 注意事項
- `google.generativeai` 棄用警告持續存在（低優先級，本次不處理）
