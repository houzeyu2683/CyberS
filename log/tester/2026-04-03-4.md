# Tester 交接文件 2026-04-03-4

## 交付對象
Developer

## 前次摘要
`2026-04-03-3.md`：完成 check_poc + check_kev 測試，以及 app.py 雙 Checkbox UI 測試。68 個測試全部通過，commit `9db4dc7` 推送至 `fresh` branch。

## 本次任務
新增兩項功能的測試程式：
1. EPSS 威脅情報指標（`check_epss`）
2. Markdown 報告產生（`report_to_markdown` + `to_markdown`）

---

## 新增 / 修改的測試檔案

### `project/tests/test_threat.py`（更新）

**新增 import**：`check_epss`

**新增 `TestCheckEpss`（6 個測試）**：
- `test_check_epss_found_low_risk`：score=0.03 → `epss_score=0.03`, `epss_high_risk=False`
- `test_check_epss_found_high_risk`：score=0.15 → `epss_score=0.15`, `epss_high_risk=True`
- `test_check_epss_not_found`：API 回傳空 data → `epss_score=None`, `epss_note="無資料"`
- `test_check_epss_request_exception`：requests 例外 → 不往外拋，回傳無資料結構
- `test_check_epss_calls_first_api`：呼叫 URL 含 `api.first.org` 與 CVE ID
- `test_check_epss_percentile_correct`：percentile 值正確

### `project/tests/test_reporter.py`（更新）

**新增 import**：`report_to_markdown`

**新增 `TestGenerateReportEpss`（4 個測試）**：
- `test_generate_report_with_epss_low_risk`：check_epss=True, score=0.03 → 含 epss_score，無 epss_warning
- `test_generate_report_with_epss_high_risk`：check_epss=True, score=0.15 → `report["epss_warning"]` 存在
- `test_generate_report_epss_no_data`：check_epss=True, 查無資料 → `threat_intel["epss_note"]=="無資料"`
- `test_generate_report_epss_false`：check_epss=False → threat_intel 不含 epss 欄位

**新增 `TestReportToMarkdown`（8 個測試）**：
- `test_report_to_markdown_basic`：回傳含 `# CVE 漏洞分析報告`
- `test_report_to_markdown_contains_cve_id`：回傳含 CVE ID
- `test_report_to_markdown_contains_risk_rating`：回傳含風險評級
- `test_report_to_markdown_with_threat_intel`：含 threat_intel → 含 `## 威脅情報`
- `test_report_to_markdown_without_threat_intel`：無 threat_intel → 不含 `## 威脅情報`
- `test_report_to_markdown_epss_warning`：epss_warning 存在 → 含 `⚠️`
- `test_report_to_markdown_returns_string`：型別為 str 且不為空
- `test_report_to_markdown_contains_summary`：含 summary 內容

### `project/tests/test_app.py`（更新）

**更新 import**：加入 `to_markdown`

**更新 `TestAnalyzeNormal`（4 個測試）**：
- `test_generate_report_called_with_cve_id`、`test_passes_check_poc_true_to_report`、
  `test_passes_check_kev_true_to_report`、`test_passes_both_true_to_report`
- 簽名改為 4 個參數 `(cve_id, check_poc, check_kev, check_epss)`
- `generate_report` 呼叫加入 `check_epss=False/True`

**新增 `TestAppUIStructure` 中 5 個 UI 測試**：
- `test_demo_contains_three_checkbox_components`：應有 3 個 Checkbox
- `test_checkbox_epss_label`：其中一個 label 含「EPSS」
- `test_demo_contains_download_button`：應有 gr.DownloadButton
- `test_demo_contains_markdown_report_button`：應有「產生 Markdown 報告」按鈕
- 原 `test_demo_contains_two_checkbox_components` 改為「三個」版本

**新增 `TestAnalyzeWithEpss`（2 個測試）**：
- `test_analyze_with_epss_true`：check_epss=True, mock 高風險 → 回傳含 epss_warning
- `test_analyze_with_epss_false`：check_epss=False → 回傳不含 epss 欄位

**新增 `TestToMarkdown`（4 個測試）**：
- `test_to_markdown_valid_report`：有效 report → md 含標題，dl_btn visible=True
- `test_to_markdown_no_report_none`：None → 提示文字，dl_btn visible=False
- `test_to_markdown_error_report`：含 error dict → dl_btn visible=False
- `test_to_markdown_creates_file`：呼叫後 `/tmp/cve_report.md` 存在

---

## 目前測試狀態

| 檔案 | 通過 | 失敗 | 原因 |
|------|------|------|------|
| test_fetcher.py | 15 | 0 | 正常 |
| test_analyzer.py | 22 | 0 | 正常 |
| test_recommender.py | 20 | 0 | 正常 |
| test_threat.py | - | ImportError | `check_epss` 尚未實作 |
| test_reporter.py | - | ImportError | `report_to_markdown`、`check_epss_fn` 尚未實作 |
| test_app.py | - | ImportError | `to_markdown` 尚未實作 |

---

## Developer 需實作的變更

### `project/kits/threat.py`（更新）—— 新增 `check_epss()`

```python
def check_epss(cve_id: str) -> dict:
    try:
        url = f"https://api.first.org/data/1.0/epss?cve={cve_id}"
        resp = requests.get(url)
        data = resp.json().get("data", [])
        if not data:
            return {"epss_score": None, "epss_percentile": None, "epss_high_risk": False, "epss_note": "無資料"}
        item = data[0]
        score = float(item["epss"])
        percentile = float(item["percentile"])
        return {
            "epss_score": score,
            "epss_percentile": percentile,
            "epss_high_risk": score > 0.1,
        }
    except Exception:
        return {"epss_score": None, "epss_percentile": None, "epss_high_risk": False, "epss_note": "無資料"}
```

### `project/kits/reporter.py`（更新）

**新增 import**：
```python
from kits.threat import check_poc as check_poc_fn, check_kev as check_kev_fn, check_epss as check_epss_fn
```

**`generate_report` 簽名更新**：
```python
def generate_report(cve_id: str, check_poc: bool = False, check_kev: bool = False, check_epss: bool = False) -> dict:
```

**threat_intel 邏輯新增**：
```python
if check_epss:
    threat_intel.update(check_epss_fn(cve_id))
```

**高風險標註（在 report 組裝後）**：
```python
if threat_intel.get("epss_high_risk"):
    score = threat_intel["epss_score"]
    report["epss_warning"] = f"高風險：EPSS 分數 {score:.4f}，超過門檻 0.1，建議優先處理"
```

**新增 `report_to_markdown(report: dict) -> str`**：
依規格產生 Markdown 字串，threat_intel 不存在時不顯示威脅情報區塊。

### `project/app.py`（更新）

**`analyze` 簽名更新**：
```python
def analyze(cve_id: str, check_poc: bool = False, check_kev: bool = False, check_epss: bool = False):
    ...
    return generate_report(cve_id.strip(), check_poc=check_poc, check_kev=check_kev, check_epss=check_epss)
```

**新增 `to_markdown` 函式**：
```python
def to_markdown(report_json):
    if not report_json or "error" in report_json:
        return "尚無報告，請先執行分析。", gr.update(visible=False)
    md_text = report_to_markdown(report_json)
    path = "/tmp/cve_report.md"
    with open(path, "w", encoding="utf-8") as f:
        f.write(md_text)
    return md_text, gr.update(value=path, visible=True)
```

**新增 EPSS Checkbox 與 Markdown UI**：
```python
epss_cb = gr.Checkbox(label="偵測 EPSS（FIRST API）", value=False)
md_btn = gr.Button("產生 Markdown 報告")
md_out = gr.Markdown()
dl_btn = gr.DownloadButton(label="下載 .md 報告", visible=False)
btn.click(fn=analyze, inputs=[inp, poc_cb, kev_cb, epss_cb], outputs=out)
md_btn.click(fn=to_markdown, inputs=[out], outputs=[md_out, dl_btn])
```

---

## 注意事項
- `check_epss` 需捕捉所有例外，不得往外拋，失敗時回傳「無資料」結構
- 高風險門檻固定為 `epss_score > 0.1`
- Markdown 下載暫存路徑：`/tmp/cve_report.md`
- `check_epss_fn` 為 reporter.py 的 module-level 名稱（測試 mock 用）
- 原有 `test_demo_contains_two_checkbox_components` 已改為三個（新增 EPSS）
