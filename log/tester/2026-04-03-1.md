# Tester 交接文件 2026-04-03-1

## 交付對象
Developer

## 任務概述
針對 Manager 交接的兩項任務，完成測試程式開發：
1. LLM SDK 替換（Anthropic → Google Generative AI）的相關測試
2. 新增 `app.py`（Gradio UI）的相關測試

---

## 修改的測試檔案

### `project/tests/test_analyzer.py`
- 將所有 `@patch("kits.analyzer.call_claude")` 改為 `@patch("kits.analyzer.call_llm")`
- 新增 `TestCallLlmAnalyzer` 測試類別，涵蓋：
  - `call_llm` 回傳正確解析的 dict
  - `call_llm` 使用 `gemini-2.0-flash` 模型
  - `call_llm` 正確傳入 `system_instruction`
  - `call_llm` API 失敗時拋出例外
  - `key.yaml` 不存在時拋出 `FileNotFoundError`
  - `GOOGLE_API_KEY` 缺失時拋出 `KeyError`

### `project/tests/test_recommender.py`
- 同上，將 `call_claude` 替換為 `call_llm`
- 新增 `TestCallLlmRecommender` 測試類別（同 analyzer 測試結構）

### `project/tests/test_app.py`（新增）
- `TestAnalyzeNormal`：合法 CVE ID → 回傳含 `cve_id, summary, impact, recommendations, risk_rating` 的 dict
- `TestAnalyzeInvalidInput`：非法格式、空字串、None → 回傳 `{"error": "..."}`
- `TestAnalyzeBoundary`：`generate_report` 拋出例外 → 回傳 `{"error": "..."}`，空白字串不呼叫 `generate_report`

---

## 目前測試狀態

| 檔案 | 通過 | 失敗 | 說明 |
|------|------|------|------|
| test_fetcher.py | 15 | 0 | 未異動，全過 |
| test_reporter.py | 29 | 2 | 2 個 integration test 需真實 API，其餘全過 |
| test_analyzer.py | 10 | 22 | 10 個輸入驗證測試通過；22 個等 Developer 實作後通過 |
| test_recommender.py | 10 | 19 | 同上 |
| test_app.py | 0 | 全失敗 | app.py 尚未建立 |

**失敗皆為預期**，原因是 `call_llm` 尚未實作、`app.py` 尚未建立。

---

## Developer 需實作的變更

### `project/kits/analyzer.py`
1. 移除 `import anthropic`
2. 加入 `import yaml, google.generativeai as genai`
3. 模組載入時讀取 `key.yaml`，呼叫 `genai.configure(api_key=...)`
   - `key.yaml` 不存在 → `FileNotFoundError` 自然拋出
   - `GOOGLE_API_KEY` 鍵不存在 → `KeyError` 自然拋出
4. 將 `call_claude(prompt: str)` 改為 `call_llm(prompt: str, system_prompt: str)`，使用 `genai.GenerativeModel`
5. `analyze_cve` 中呼叫改為 `call_llm(prompt, SYSTEM_PROMPT)`

### `project/kits/recommender.py`
同上邏輯

### `project/app.py`（新增）
```python
import gradio as gr
from kits.reporter import generate_report

def analyze(cve_id: str):
    try:
        return generate_report(cve_id)
    except Exception as e:
        return {"error": str(e)}

with gr.Blocks() as demo:
    inp = gr.Textbox(label="CVE ID", placeholder="CVE-2021-44228")
    btn = gr.Button("分析")
    out = gr.JSON()
    btn.click(fn=analyze, inputs=inp, outputs=out)

if __name__ == "__main__":
    demo.launch()
```

---

## 注意事項
- `key.yaml` 路徑需使用相對路徑，從 `project/` 目錄執行
- `project/.gitignore` 需確認 `key.yaml` 已排除
- 既有 59 個單元測試（test_fetcher + test_reporter unit）在 Developer 實作完成後應維持通過
