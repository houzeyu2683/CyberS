# Tester 交接文件 2026-04-03-2

## 交付對象
Developer

## 前次摘要
`2026-04-03-1.md`：完成 LLM SDK 替換測試與 app.py 基本測試開發。

## 本次任務
新增需求：`app.py` 的 Gradio 頁面加入中文說明區塊（`gr.Markdown`）。

---

## 修改的測試檔案

### `project/tests/test_app.py`

新增 import：`import gradio as gr`、`from app import analyze, demo`

新增 `TestAppUIStructure` 測試類別（10 個測試），涵蓋：

| 測試 | 說明 |
|------|------|
| `test_demo_is_gradio_blocks` | demo 為 gr.Blocks 實例 |
| `test_demo_contains_markdown_component` | demo 包含 gr.Markdown |
| `test_demo_contains_textbox_component` | demo 包含 gr.Textbox |
| `test_demo_contains_json_component` | demo 包含 gr.JSON |
| `test_markdown_contains_project_title` | Markdown 含「CVE 自動分析系統」 |
| `test_markdown_contains_usage_flow` | Markdown 含「使用流程」 |
| `test_markdown_contains_three_steps` | Markdown 含 1. 2. 3. 三步驟 |
| `test_markdown_mentions_project_purpose` | Markdown 含「專案用途」 |
| `test_textbox_label_is_cve_id` | Textbox label 為「CVE ID」 |
| `test_textbox_placeholder_contains_example` | Textbox placeholder 含「CVE-」 |

---

## 目前測試狀態

| 檔案 | 通過 | 失敗 | 說明 |
|------|------|------|------|
| test_fetcher.py | 15 | 0 | 正常 |
| test_reporter.py | 29 | 2 | 2 個 integration 需真實 API |
| test_app.py | 19 | 5 | 5 個 Markdown 測試等實作 |
| test_analyzer.py | 10 | 22 | 等 call_llm 實作 |
| test_recommender.py | 10 | 19 | 同上 |

---

## Developer 需實作的變更

### `project/app.py`
在 `with gr.Blocks() as demo:` 內、`inp` 之前加入：
```python
gr.Markdown("""
## CVE 自動分析系統

**專案用途**
輸入 CVE ID，系統自動從 NVD、OSV.dev、GitHub Advisory 擷取漏洞資訊，
透過 AI 分析影響範圍與攻擊向量，並生成具體的修復建議。

**使用流程**
1. 在下方輸入框填入 CVE ID（例：CVE-2021-44228）
2. 點擊「分析」按鈕
3. 查看右側 JSON 報告，包含漏洞摘要、影響分析、修復建議與風險評級
""")
```

### `project/kits/analyzer.py` 與 `project/kits/recommender.py`
（同 `2026-04-03-1.md` 交接內容，無變更）

---

## 注意事項
- 測試使用 `gemini-2.5-flash`（非 Manager 文件的 `gemini-2.0-flash`），Developer 實作時請以測試為準
- `google.generativeai` 套件已廢棄（FutureWarning），改用 `google.genai`，但不影響現有測試執行
- `app.py` 已存在，Developer 只需在 `gr.Blocks()` 內加入 `gr.Markdown`
