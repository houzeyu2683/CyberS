# Tester 交接文件 2026-04-03-3

## 交付對象
Developer

## 前次摘要
`2026-04-03-2.md`：完成 gr.Markdown UI 測試。

## 本次任務
新增威脅情報功能：`kits/threat.py`（check_poc + check_kev）、`reporter.py` 新增參數、`app.py` 新增兩個 Checkbox。

---

## 新增 / 修改的測試檔案

### `project/tests/test_threat.py`（新增）
測試 `kits/threat.py` 中的兩個函式：

**`TestCheckPocNormal`（6 個測試）**
- 回傳含 `has_poc`、`poc_count` 的 dict
- `total_count > 0` → `has_poc: True`
- `total_count == 0` → `has_poc: False`，`poc_count: 0`
- 呼叫 `api.github.com/search/repositories` 並帶入 CVE ID
- `GITHUB_TOKEN` 存在 → Authorization header 包含 token
- `GITHUB_TOKEN` 不存在 → 無 Authorization header

**`TestCheckPocError`（2 個測試）**
- `ConnectionError` → 拋出 `ConnectionError`

**`TestCheckKevNormal`（6 個測試）**
- 回傳含 `in_the_wild` 的 dict
- CVE 在清單 → `True`；不在清單 → `False`；空清單 → `False`
- 呼叫 `cisa.gov/.../known_exploited_vulnerabilities.json`
- CVE ID 比對不分大小寫

**`TestCheckKevError`（2 個測試）**
- `ConnectionError` → 拋出 `ConnectionError`

### `project/tests/test_reporter.py`（更新）
新增 `TestGenerateReportThreatIntel`（5 個測試）：
- `check_poc=False, check_kev=False` → 報告無 `threat_intel`
- `check_poc=True` → 含 `has_poc`，無 `in_the_wild`（mock `kits.reporter.check_poc_fn`）
- `check_kev=True` → 含 `in_the_wild`，無 `has_poc`（mock `kits.reporter.check_kev_fn`）
- 兩者都 True → 完整 `threat_intel`
- 預設呼叫（無參數）→ 無 `threat_intel`（向下相容）

### `project/tests/test_app.py`（更新）
- `analyze` 呼叫簽名改為 `analyze(cve_id, check_poc, check_kev)`
- 新增 4 個 `analyze` 參數傳遞測試（check_poc/kev 正確傳入 generate_report）
- 新增 4 個 Checkbox UI 測試：
  - 包含 2 個 gr.Checkbox
  - PoC Checkbox label 含「PoC」
  - KEV Checkbox label 含「KEV」或「Wild」
  - 兩個 Checkbox 預設值為 False

---

## 目前測試狀態

| 檔案 | 通過 | 失敗 | 原因 |
|------|------|------|------|
| test_fetcher.py | 15 | 0 | 正常 |
| test_reporter.py | 17 | 4 | threat_intel 測試等實作 |
| test_app.py | 15 | 16 | analyze 簽名 + Checkbox 等實作 |
| test_threat.py | 0（import 失敗） | - | kits/threat.py 尚未建立 |
| test_analyzer.py | 10 | 22 | call_llm 等實作 |
| test_recommender.py | 10 | 19 | call_llm 等實作 |

---

## Developer 需實作的變更

### `project/kits/threat.py`（新增）
```python
import requests
import yaml

try:
    with open("key.yaml") as f:
        keys = yaml.safe_load(f)
    GITHUB_TOKEN = keys.get("GITHUB_TOKEN")
except FileNotFoundError:
    GITHUB_TOKEN = None

def check_poc(cve_id: str) -> dict:
    headers = {}
    if GITHUB_TOKEN:
        headers["Authorization"] = f"token {GITHUB_TOKEN}"
    url = f"https://api.github.com/search/repositories?q={cve_id}"
    resp = requests.get(url, headers=headers)
    data = resp.json()
    count = data.get("total_count", 0)
    return {"has_poc": count > 0, "poc_count": count}

def check_kev(cve_id: str) -> dict:
    url = "https://www.cisa.gov/sites/default/files/feeds/known_exploited_vulnerabilities.json"
    resp = requests.get(url)
    vulns = resp.json().get("vulnerabilities", [])
    found = any(v.get("cveID", "").upper() == cve_id.upper() for v in vulns)
    return {"in_the_wild": found}
```

### `project/kits/reporter.py`
```python
from kits.threat import check_poc as check_poc_fn, check_kev as check_kev_fn

def generate_report(cve_id: str, check_poc: bool = False, check_kev: bool = False) -> dict:
    ...
    threat_intel = {}
    if check_poc:
        threat_intel.update(check_poc_fn(cve_id))
    if check_kev:
        threat_intel.update(check_kev_fn(cve_id))
    if threat_intel:
        report["threat_intel"] = threat_intel
    return report
```

### `project/app.py`
```python
def analyze(cve_id: str, check_poc: bool, check_kev: bool):
    ...
    return generate_report(cve_id.strip(), check_poc=check_poc, check_kev=check_kev)

with gr.Blocks() as demo:
    ...
    poc_cb = gr.Checkbox(label="偵測 PoC（GitHub Search API）", value=False)
    kev_cb = gr.Checkbox(label="偵測 In the Wild（CISA KEV）", value=False)
    btn.click(fn=analyze, inputs=[inp, poc_cb, kev_cb], outputs=out)
```
Markdown 說明區塊需加入兩個選項的說明。

---

## 注意事項
- `check_poc_fn` / `check_kev_fn` 是 reporter.py 的 module-level 名稱（測試 mock 用）
- `GITHUB_TOKEN` 為選填，無此 key 時應靜默處理（非拋出例外）
- `test_checkboxes_default_to_false` 測試 Checkbox `value` 屬性
