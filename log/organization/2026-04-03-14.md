# 專案進度摘要 2026-04-03-14

## 前次摘要（2026-04-03-13）
Deployer 完成威脅情報功能（PoC + KEV），68 個測試通過，commit `9db4dc7` 推送至 `fresh` branch。

## 本次進度（Manager）

### 規劃兩項新功能

**功能一：EPSS 威脅情報指標**
- 新增 `check_epss()` 至 `kits/threat.py`，呼叫 FIRST API
- 回傳 epss_score、epss_percentile、epss_high_risk（門檻 > 0.1）
- 查無資料或例外回傳「無資料」
- 高風險時在報告中新增 epss_warning 欄位
- `generate_report()` 新增 `check_epss` 參數
- `app.py` 新增第三個 Checkbox

**功能二：Markdown 報告產生**
- 新增 `report_to_markdown()` 至 `kits/reporter.py`
- 流程：先分析取得 JSON → 按「產生 Markdown 報告」→ 顯示渲染 + 下載 .md
- `app.py` 新增「產生 Markdown 報告」按鈕、`gr.Markdown` 輸出、`gr.DownloadButton`

### 討論紀錄
- RAG 功能：目前無內部文件，暫不納入，待有內部知識庫需求再規劃

## 下一步
交接給 Tester，開發測試程式。
