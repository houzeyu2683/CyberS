# 專案進度摘要 2026-04-03-16

## 前次摘要（2026-04-03-15）
Tester 完成 EPSS + Markdown 報告功能測試程式（新增 29 個測試），三個測試檔 ImportError 等待實作。

## 本次進度（Developer）

### 實作項目
- `kits/threat.py`：新增 `check_epss()`，呼叫 FIRST EPSS API
- `kits/reporter.py`：`generate_report()` 支援 `check_epss`，新增 `report_to_markdown()`
- `app.py`：新增 EPSS Checkbox、`to_markdown()`、Markdown 輸出區、下載按鈕

### 測試結果
**96/96 全數通過**（57 原有 + 29 新增 + 10 更新）

## 下一步
交接給 Checker 進行功能驗證。
