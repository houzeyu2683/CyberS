# 專案進度摘要 2026-04-03-4

## 上次摘要
`2026-04-03-3.md`：新增 threat_intel（PoC + KEV），app.py 加入兩個 Checkbox。

## 本次變更
新增 EPSS（Exploit Prediction Scoring System）作為第三個威脅情報來源：
- `kits/threat.py` 新增 `check_epss()`，呼叫 FIRST.org 公開 API
- `reporter.py` 新增 `check_epss` 參數
- `recommender.py` 將 threat_intel 注入 LLM prompt，使優先級建議有數據依據
- `app.py` 新增第三個 Checkbox

## 架構決策
EPSS + KEV + PoC 三個維度組合符合 2026 業界標準，LLM 的價值從「摘要生成」升級為「多維度數據推理」。

## 下一步
Tester 依據 `log/manager/2026-04-03-4.md` 開發測試，交接給 Developer 實作。
