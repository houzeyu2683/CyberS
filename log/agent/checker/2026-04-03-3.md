# Checker 交接文件 2026-04-03-3

## 交付對象
Deployer

## 任務概述
依據 Developer 交接文件（`log/developer/2026-04-03-3.md`），驗證威脅情報功能新增。

---

## 檢查清單與結果

### 1. `kits/threat.py`
- [x] `check_poc(cve_id)`：呼叫 GitHub Search API，回傳 `{"has_poc": bool, "poc_count": int}` ✓
- [x] `check_kev(cve_id)`：呼叫 CISA KEV，比對 CVE ID（不分大小寫），回傳 `{"in_the_wild": bool}` ✓
- [x] `GITHUB_TOKEN`：`key.yaml` 存在時讀取，`FileNotFoundError` 時靜默設為 `None` ✓
- [x] 有 token → `Authorization: token {GITHUB_TOKEN}`；無 token → 無 header ✓

### 2. `kits/reporter.py`
- [x] `from kits.threat import check_poc as check_poc_fn, check_kev as check_kev_fn` ✓
- [x] `generate_report` 簽名：`(cve_id, check_poc=False, check_kev=False)` ✓
- [x] `check_poc=True` → 呼叫 `check_poc_fn`，`update` 至 `threat_intel` ✓
- [x] `check_kev=True` → 呼叫 `check_kev_fn`，`update` 至 `threat_intel` ✓
- [x] `threat_intel` 非空 → 加入回傳 dict；兩者皆 False → 不含 `threat_intel` ✓

### 3. `app.py`
- [x] `analyze` 簽名：`(cve_id, check_poc=False, check_kev=False)` ✓
- [x] `generate_report(cve_id.strip(), check_poc=check_poc, check_kev=check_kev)` ✓
- [x] `poc_cb = gr.Checkbox(label="偵測 PoC（GitHub Search API）", value=False)` ✓
- [x] `kev_cb = gr.Checkbox(label="偵測 In the Wild（CISA KEV）", value=False)` ✓
- [x] `btn.click(fn=analyze, inputs=[inp, poc_cb, kev_cb], outputs=out)` ✓
- [x] Markdown 新增「威脅情報選項」說明段落 ✓

### 4. 測試結果
```
68 passed, 2 warnings in 93.72s
```
warnings 皆為持續存在的既有警告，無新增問題。

---

## Deployer 需執行的項目

1. 提交以下變更至 git：
   - `project/kits/threat.py`（新增）
   - `project/kits/reporter.py`（新增 threat_intel 邏輯）
   - `project/app.py`（新增 Checkbox、更新 analyze 簽名）
   - `project/tests/test_threat.py`（新增）
   - `project/tests/test_reporter.py`（新增 threat_intel 測試）
   - `project/tests/test_app.py`（新增 Checkbox 測試）
2. 推送至遠端倉庫
